let initialState = {
    friends: [
         {id: 2,  name: 'Dima' },  
         {id: 3, name: 'Oleg' }, 
         {id: 4, name: 'Sasha'}
    ]
}

const sidebarReducer = (state = initialState, action) => {

    return state
}

export default sidebarReducer